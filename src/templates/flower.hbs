<ul class="flowers">
    {{#flowers}}
    <li>
      <h3 class="flowers__name">{{name}}</h3>
      <img src={{img}} alt={{name}} class="flowers__photo" />
      <p class="flowers__description">{{description}}</p>
    </li>
  {{/flowers}}
  </ul>